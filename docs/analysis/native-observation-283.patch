diff --git a/crates/overdrive-control-plane/src/handlers.rs b/crates/overdrive-control-plane/src/handlers.rs
index 0538fcfe..61ef06b4 100644
--- a/crates/overdrive-control-plane/src/handlers.rs
+++ b/crates/overdrive-control-plane/src/handlers.rs
@@ -72,6 +72,7 @@ fn enqueue_workload_lifecycle_eval(
     let target_string = format!("workload/{workload_id}");
     let target = TargetResource::new(&target_string)
         .map_err(|e| ControlPlaneError::internal("TargetResource::new(workload/<id>)", e))?;
+    tracing::info!(workload = %workload_id, "issue283 handler enqueue");
     state.runtime.broker().submit(Evaluation { reconciler, target });
     Ok(())
 }
diff --git a/crates/overdrive-control-plane/src/lib.rs b/crates/overdrive-control-plane/src/lib.rs
index 36c134f2..46977b97 100644
--- a/crates/overdrive-control-plane/src/lib.rs
+++ b/crates/overdrive-control-plane/src/lib.rs
@@ -3363,6 +3363,7 @@ fn spawn_convergence_loop(
             };
 
             for eval in pending {
+                tracing::info!(reconciler = %eval.reconciler, target_name = %eval.target.as_str(), tick_n, "issue283 convergence evaluation enter");
                 if let Err(e) = run_convergence_tick(
                     &state,
                     &eval.reconciler,
diff --git a/crates/overdrive-host/src/vmm.rs b/crates/overdrive-host/src/vmm.rs
index e34a961b..da79302b 100644
--- a/crates/overdrive-host/src/vmm.rs
+++ b/crates/overdrive-host/src/vmm.rs
@@ -479,6 +479,7 @@ impl Vmm for CloudHypervisorVmm {
                 "spawned cloud-hypervisor child reported no pid".to_string(),
             ));
         };
+        tracing::info!(alloc = %config.alloc, pid, "issue283 VMM process spawned");
         let stderr_pipe = child.stderr.take();
 
         let (exit_tx, exit_rx) = oneshot::channel::<VmmExit>();
@@ -519,6 +520,7 @@ impl Vmm for CloudHypervisorVmm {
             // The final tail is read back off the capture AFTER the last
             // append, never separately assembled.
             let vmm_exit = classify_exit(status, exit_diagnostics.console_tail());
+            tracing::info!(pid, ?vmm_exit, "issue283 VMM reaped");
             let _ = exit_tx.send(vmm_exit.clone());
             let _ = state.outcome.send(Some(vmm_exit));
             live.lock().remove(&pid);
diff --git a/crates/overdrive-worker/src/vm_driver.rs b/crates/overdrive-worker/src/vm_driver.rs
index 04d1365a..b93449d1 100644
--- a/crates/overdrive-worker/src/vm_driver.rs
+++ b/crates/overdrive-worker/src/vm_driver.rs
@@ -774,8 +774,9 @@ async fn run_beacon_writer(mut state: BeaconWriterTaskState, mut stop: watch::Re
     loop {
         if *stop.borrow() {
             if let Some(write_half) = state.write_half.as_mut() {
-                let _ = write_half.write_all(b"SHUTDOWN\n").await;
-                let _ = write_half.flush().await;
+                let written = write_half.write_all(b"SHUTDOWN\n").await;
+                let flushed = write_half.flush().await;
+                tracing::info!(pid = state.control.pid, ?written, ?flushed, "issue283 SHUTDOWN write finished");
             }
             return;
         }
@@ -783,8 +784,9 @@ async fn run_beacon_writer(mut state: BeaconWriterTaskState, mut stop: watch::Re
             biased;
             () = wait_for_signal(&mut stop) => {
                 if let Some(write_half) = state.write_half.as_mut() {
-                    let _ = write_half.write_all(b"SHUTDOWN\n").await;
-                    let _ = write_half.flush().await;
+                    let written = write_half.write_all(b"SHUTDOWN\n").await;
+                    let flushed = write_half.flush().await;
+                    tracing::info!(pid = state.control.pid, ?written, ?flushed, "issue283 SHUTDOWN write finished");
                 }
                 return;
             }
@@ -1340,6 +1342,7 @@ impl VmDriver {
                 .await);
         }
 
+        tracing::info!(alloc = %spec.alloc, "issue283 host preparation complete; VMM create enter");
         let (control, exit, diagnostics) = match self.vmm.create(&config).await {
             Ok(process) => (process.control, process.exit, process.diagnostics),
             Err(err) => {
@@ -1454,6 +1457,7 @@ impl Driver for VmDriver {
     }
 
     async fn start(&self, spec: &AllocationSpec) -> Result<AllocationHandle, DriverError> {
+        tracing::info!(alloc = %spec.alloc, "issue283 VM start enter");
         let ProvisionedVmm {
             run_dir,
             listener,
@@ -1464,6 +1468,7 @@ impl Driver for VmDriver {
             rootfs,
             memory,
         } = self.provision_vmm(spec).await?;
+        tracing::info!(alloc = %spec.alloc, pid = control.pid, "issue283 VMM create complete");
 
         // Transition Starting -> Live (still §103 step 0's Held phase;
         // brief §105a.3's transition table treats Starting|Live as one
@@ -1512,6 +1517,7 @@ impl Driver for VmDriver {
 
         match outcome {
             BootRaceOutcome::Beacon(Ok((reader, write_half))) => {
+                tracing::info!(alloc = %spec.alloc, pid = control.pid, "issue283 READY accepted");
                 // ADR-0089 §1 / Q9: retain the existing EXEC reply on the
                 // guest-initiated session, but do NOT write it here. `start`
                 // returns once READY is accepted; the action shim installs
@@ -1649,6 +1655,7 @@ impl Driver for VmDriver {
     }
 
     async fn stop(&self, handle: &AllocationHandle) -> Result<(), DriverError> {
+        tracing::info!(alloc = %handle.alloc, pid = ?handle.pid, "issue283 VM stop enter");
         let extracted = {
             let mut live = self.live.lock();
             let fields = match live.get_mut(&handle.alloc) {
@@ -1738,7 +1745,9 @@ impl Driver for VmDriver {
         // `Vmm::terminate` is idempotent on an already-dead process
         // (S-VM-76 sequence (c)) — `Ok(VmTermination::Killed)`, never an
         // error.
-        let _ = self.vmm.terminate(&control, VM_STOP_GRACE).await;
+        tracing::info!(alloc = %handle.alloc, pid = control.pid, "issue283 request window complete; terminate enter");
+        let termination = self.vmm.terminate(&control, VM_STOP_GRACE).await;
+        tracing::info!(alloc = %handle.alloc, pid = control.pid, ?termination, "issue283 terminate complete; cleanup enter");
 
         // Step 3: tear down the host footprint. Best-effort — benign if
         // already gone (a concurrent double-stop, S-VM-76 sequence (d)).
@@ -1754,5 +1763,7 @@ impl Driver for VmDriver {
         let _ = tokio::fs::remove_dir_all(run_dir.path()).await;
         remove_clone_then_index_link(&rootfs).await;
 
+        tracing::info!(alloc = %handle.alloc, pid = control.pid, "issue283 VM stop cleanup complete");
+
         Ok(())
     }
