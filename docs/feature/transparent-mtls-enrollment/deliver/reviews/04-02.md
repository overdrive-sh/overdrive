# Adversarial Review — Step 04-02 (per-connection MtlsResolve consumer + single-cut declared-peer deletion)

- **Artifact**: implementation @ commit `a8ff4722` (on top of 04-01 `4fe2320f`)
- **Reviewer**: `nw-software-crafter-reviewer` (adversarial, Opus via rigor `reviewer_model: inherit`)
- **Date**: 2026-06-20
- **Mutation gate**: running (diff-scoped, `--package overdrive-worker --file mtls_intercept_worker.rs`); empirical result appended below when complete. Reviewer's static prediction: ≥80%.
- **Verdict**: **APPROVED** (zero defects)

---

## Method

Ground-truthed every adversarial-focus claim against the source (this feature has a
documented false-`COMMIT-EXECUTED-PASS` history; the execution log was not trusted). The
security-critical 3-arm decision, the deletion completeness, the composition-root wiring,
and the three crafter-surfaced judgment calls were each read line-by-line.

## Adversarial focus — all refuted, no defect

1. **Confidentiality invariant (3-arm cannot leak cleartext) — VERIFIED SAFE.**
   `decide_outbound(&MtlsResolution) -> OutboundAction` (mtls_intercept_worker.rs:792-798)
   is a pure exhaustive match on the **variant** (C1 — no sentinel/`Option::is_some`
   inference). `handle_outbound` (633-693): `Mesh(b)` → `enforce` with
   `Routed::Outbound { peer: b.addr }` (the *resolved* backend addr, not orig_dst;
   `expected_peer: None` — authn-only); `NonMesh` → cleartext pass-through (by design);
   `MeshUnreachable` → `drop(leg_f)`, no dial, NO cleartext. No path leaks cleartext to a
   should-be-mesh peer.

2. **Per-arm tests genuine, not Fixture Theater — VERIFIED.** A pure `decide_outbound`
   3-arm test (asserts each arm's distinct `OutboundAction`) + four integrated port-to-port
   arm tests against `SimMtlsResolve` + a recording spy enforcement: `Mesh →
   enforce-with-peer==b.addr` (1189), `NonMesh → upstream-receives-bytes` (1278),
   `MeshUnreachable → client-EOF` (1305), `StoreFault → client-EOF` (1333). A
   `FailClosed→PassThrough` collapse is caught by the EOF assertion. Each arm independently
   killable.

3. **Deletion completeness (single-cut) — VERIFIED CLEAN.** `real_peer`,
   `accept_drop_outbound`, `AcceptOutcome::Dropped` (the whole `AcceptOutcome` enum — its
   `ListenerClosed` role is carried by the surviving `ConnectionReady::ListenerClosed`, a
   clean rename), `MtlsInterceptError`, `program_declared_peer_redirect` — zero orphans in
   `crates/`, no `#[allow(dead_code)]`, no stubs. Load-bearing survivor intact: the
   install-time local `leg_f_addr` feeding `install_outbound_tproxy(host_veth,
   leg_f_addr.port())` (351, 373). `expected_peer` survives (authn-only). The criterion-3
   broken intra-doc link was already fixed (lib.rs:980-984, clean Debug docstring).

4. **Composition-root wiring — VERIFIED CORRECT.** lib.rs:1828-1933, strict order:
   construct enforcement → `enforcement.probe()` → construct `ServiceBackendsResolve` →
   `resolve.probe()` → construct worker. Probe before serve (Earned-Trust wire→probe→use);
   `MtlsResolve` mandatory `new()` param (no builder/default); both refusal paths emit
   `health.startup.refused` + typed errors.

5. **The three surfaced judgment calls — rulings:**
   - **(i) resolve-probe-before-serve e2e → LEGITIMATE Tier-3 deferral to 05-01.** The
     `compose_mtls` branch is reachable only with real kTLS (the enforcement probe gates
     it, runs first); 04-01 deleted the `compose_mtls=true` e2e fixtures. A default-lane
     test could reach it only by faking the enforcement probe (sim-in-production-boot) or a
     contrived reorder — both forbidden (development.md § "Production code is not shaped by
     simulation"). The `ServiceBackendsResolve::probe` refusal itself IS covered
     (mtls_resolve_adapter tests + 01-03). 05-01 (real kTLS) is the right home for the
     composed wire→probe→serve assertion. **Obligation carried into the 05-01 dispatch.**
   - **(ii) `MtlsBootError::ResolveProbe` → CORRECT typed-error pattern** (error.rs:393-397),
     mirrors the sibling `::Probe`, embeds `MtlsResolveError` via `#[source]` (no
     `Internal(String)` flattening). Sanctioned, not invented surface.
   - **(iii) store-`Err` (StoreUnreadable) treated FAIL-CLOSED → CORRECT.** The contract is
     asymmetric: a "should-be-mesh-but-can't" classifies into `Ok(MeshUnreachable)`; only a
     store FAULT returns `Err(StoreUnreadable)`. A miss is `NonMesh` by design (an `Ok`
     arm), so it does not collide with the Err path. Fail-closed-on-fault is the
     confidentiality-correct posture; the availability trade-off (transient store hiccup
     drops outbound) is the intended v1 stance pending #236's fail-toward-handshake.

6. **Authn-only boundary — VERIFIED.** `expected_peer: None` at every construction site; no
   `IdentityRead` threaded; no test calls the wrong-but-valid-peer case "protected".

## Recurring-defect-class audit
False-COMMIT-PASS, vacuous assertions, Fixture Theater, stale-docs-from-deletions — all
refuted. (The one stale item was in the review brief's premise about the `AcceptOutcome`
name, not in the code.)

## Verdict: APPROVED

Clean, defect-free. The mutation gate (running) will empirically confirm the ≥80% prediction
on the 3-arm logic.

---

## Mutation gate result

_(pending — diff-scoped run on `overdrive-worker` / `mtls_intercept_worker.rs`; appended on completion)_
