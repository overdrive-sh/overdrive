{
  "roadmap": {
    "project_id": "dial-by-name-responder",
    "created_at": "2026-06-24T19:56:12Z",
    "total_steps": 9,
    "revised_at": "2026-06-25T00:00:00Z",
    "revision": "REV-2 (ADR-0072 stable-frontend / ClusterIP split) — adds 01-04 (FrontendAddrAllocator) + 02-00 (MtlsResolve re-key); re-scopes 01-03 (<job>->stable F, withhold-not-release), 02-01 (+FrontendAddrAllocator + re-keyed MtlsResolve wiring), 02-02 (stable-F + churn ACs), 03-01 (withhold-not-release); 01-01/01-02/03-02 unchanged. 39 S-DBN-* scenarios (was 26)."
  },
  "phases": [
    {
      "id": "01",
      "name": "Pure seams (Tier-1 foundations)",
      "steps": [
        {
          "id": "01-01",
          "name": "MeshServiceName newtype — the <job>.svc.overdrive.local grammar",
          "criteria": [
            "S-DBN-NAME-01: a mandatory proptest round-trip in crates/overdrive-core/tests/acceptance/core_newtype_roundtrip.rs proves that for every valid <job> label L (1..=LABEL_MAX after the fixed '.svc.overdrive.local' suffix budget), a MeshServiceName built from '<L>.svc.overdrive.local' survives Display->FromStr (re-parse equals original), as_str() yields the canonical lowercase <job> label, and serde_json::to_string/from_str round-trips to the quoted Display form (serde matches Display/FromStr exactly, the mandatory newtype rule per .claude/rules/testing.md § 'Newtype roundtrip'). proptest is the paradigm here, not single #[test].",
            "S-DBN-NAME-02: a proptest in core_newtype_roundtrip.rs proves FromStr/new is case-insensitive — for every case-permutation P of '<L>.svc.overdrive.local' (mixed case in BOTH label and suffix), MeshServiceName::new(P) succeeds and equals new of the all-lowercase form, and Display emits the lowercase canonical (matches the validate_label precedent at id.rs:99).",
            "S-DBN-NAME-03: an example-matrix #[test] in crates/overdrive-core/tests/acceptance/core_newtype_validation.rs proves the bespoke suffix FromStr accepts 'server.svc.overdrive.local' (<job>=server) and 'payments-api.svc.overdrive.local', and rejects wrong suffix ('server.svc.example.com'), non-terminal suffix ('server.svc.overdrive.local.evil'), missing suffix ('server'), missing .svc segment ('server.overdrive.local'), and empty <job> ('.svc.overdrive.local') — each rejection returning Err(IdParseError::<variant>); the per-case variant is a DELIVER refinement, the test asserts is_err()+IdParseError and pins the accepted-case <job> extraction.",
            "S-DBN-NAME-04: a proptest in core_newtype_validation.rs proves every label violating DNS-1123 rules (empty, > LABEL_MAX after suffix budget, leading/trailing non-alphanumeric, out-of-class char) returns Err(IdParseError::<variant>) — never panics, never silently truncates; the ceiling is sized off overdrive_core::id::LABEL_MAX (253), never a bespoke smaller magic number (development.md § 'One shared length ceiling').",
            "Pinned surface ONLY (CLAUDE.md 'Implement to the design'): MeshServiceName lands in crates/overdrive-core/src/id.rs with const SUFFIX = \"svc.overdrive.local\", new(&str) -> Result<Self, IdParseError>, as_str(&self) -> &str, + Display (lowercase) + FromStr (case-insensitive) + Serialize/Deserialize (matching) + TryFrom<String>/<&str>; modeled on define_label_newtype! + validate_label + LABEL_MAX with a bespoke suffix FromStr. Invent NO public surface beyond this.",
            "RED convention: the production stub lands RED via todo!(\"RED scaffold: …\") + a file/module-scoped #![cfg_attr(not(test), expect(clippy::todo, …))]; the test scaffolds land RED via #[should_panic(expected = \"RED scaffold\")]; the per-step RED->GREEN->COMMIT cycle (ADR-025) flips them, fail-for-right-reason gated by distill/red-classification.md.",
            "Mutation gate: a per-step diff-scoped run — cargo xtask lima run -- cargo xtask mutants --diff origin/main --features integration-tests --package overdrive-core --file crates/overdrive-core/src/id.rs — achieves kill-rate >= 80%, targeting MeshServiceName::new validation (suffix-grammar branch + label-limit) and the case-fold/lowercase-Display path."
          ],
          "test_file": "crates/overdrive-core/tests/acceptance/core_newtype_roundtrip.rs",
          "scenario_name": "S-DBN-NAME-01/02/03/04 — MeshServiceName round-trips, case-folds, and rejects bad grammar/labels"
        },
        {
          "id": "01-02",
          "name": "NameAnswer enum + hickory-proto wire codec (encode/decode)",
          "criteria": [
            "S-DBN-WIRE-01: a proptest (symmetric round-trip) in crates/overdrive-control-plane/tests/acceptance/dns_wire.rs proves that for every query name N and non-empty SocketAddrV4 set, wire::encode(N, A, NameAnswer::Records(addrs)) -> bytes decoded by hickory_proto's Message parser yields ResponseCode::NoError, exactly one A record per addr, the decoded A-record set equals the input set, and ANCOUNT == addrs.len(). proptest is the paradigm.",
            "S-DBN-WIRE-02: a proptest in dns_wire.rs proves wire::encode(N, AAAA, NameAnswer::NoData) -> bytes decoded by hickory yields ResponseCode::NoError, ANCOUNT == 0, exactly one SOA in the AUTHORITY section, and that SOA's MINIMUM (RFC 2308 negative TTL) == 1 (DDN-8).",
            "S-DBN-WIRE-03: a proptest in dns_wire.rs proves that for every name N and qtype Q in {A, AAAA}, wire::encode(N, Q, NameAnswer::NxDomain) -> bytes decoded by hickory yields ResponseCode::NXDomain, ANCOUNT == 0, and exactly one SOA in AUTHORITY with MINIMUM == 1 (the negative-TTL SOA so the stub resolver re-resolves once a backend is running-and-healthy).",
            "S-DBN-WIRE-04: a proptest in dns_wire.rs proves the SOA SERIAL is a deterministic function of an injected Arc<dyn Clock> reading T — two encodes with the SAME T produce byte-identical SOA SERIAL, two encodes with DISTINCT T (differing past the SERIAL granularity, pinned in the @example per the DISTILL reviewer suggestion #3) produce distinct SERIALs; the encoder NEVER reads wall-clock (development.md § 'Never call SystemTime::now() in core logic').",
            "Pinned surface ONLY: enum NameAnswer { Records(Vec<SocketAddrV4>), NoData, NxDomain } lands in overdrive-core (id.rs or a small dns module — DESIGN's own intentional latitude per ADR-0072 § Components, DELIVER picks one and documents it, inventing no surface); the variant names ARE the contract. NameAnswer stays hickory-free; only the qtype (hickory_proto::rr::RecordType) crosses the wire.rs ACL boundary. The wire codec lands in crates/overdrive-control-plane/src/dns_responder/wire.rs as hickory-proto decode (query) + encode (A / NODATA-SOA / NXDOMAIN-SOA, MINIMUM=1, SERIAL via Clock).",
            "Wiring step (DELIVER, per ADR-0072 § Components): ADD hickory-proto (Apache-2.0/MIT) to root Cargo.toml [workspace.dependencies] and pull it into overdrive-control-plane — this is why these scaffolds could not land mid-DISTILL. The dns_responder/mod.rs module wiring + re-exports land here.",
            "RED convention: production stubs via todo!(\"RED scaffold: …\") + #![cfg_attr(not(test), expect(clippy::todo, …))] on the dns_responder module; dns_wire.rs scaffolds via #[should_panic(expected = \"RED scaffold\")]; RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate: a per-step diff-scoped run — cargo xtask lima run -- cargo xtask mutants --diff origin/main --features integration-tests --package overdrive-control-plane --file crates/overdrive-control-plane/src/dns_responder/wire.rs (and --package overdrive-core --file crates/overdrive-core/src/id.rs if NameAnswer lands there) — achieves kill-rate >= 80%, targeting the SOA/MINIMUM=1, the SERIAL-from-Clock derivation, and the NoError/NXDomain response-code arms."
          ],
          "test_file": "crates/overdrive-control-plane/tests/acceptance/dns_wire.rs",
          "scenario_name": "S-DBN-WIRE-01/02/03/04 — A round-trip, NODATA-SOA, NXDOMAIN-SOA (MINIMUM=1), SERIAL via Clock"
        },
        {
          "id": "01-04",
          "name": "FrontendAddrAllocator — stable per-<job> frontend addr in 10.98.0.0/16 (1a-A)",
          "criteria": [
            "S-DBN-FRONTEND-01: a proptest in crates/overdrive-control-plane/tests/acceptance/dns_frontend_allocator.rs proves that for every <job> label J, a fresh FrontendAddrAllocator's assign(J) returns Ok(F) where F is an Ipv4Addr AND F is a member of WORKLOAD_FRONTEND_BASE (10.98.0.0/16) AND F is disjoint from WORKLOAD_SUBNET_BASE (10.99.0.0/16, the per-netns /30s at veth_provisioner.rs:307) AND F is disjoint from the service-VIP range (10.96.0.0/16, VipRange::default()) — the membership is asserted against the two REAL named consts, never a magic number (this is the collision the spike's 10.96.0.0/16 candidate was REJECTED for, ADR-0072 § Collision check). @example: J=server, F is the allocator's first stable assignment in 10.98.0.0/16. proptest is the paradigm.",
            "S-DBN-FRONTEND-02: a proptest in dns_frontend_allocator.rs proves the addr is RETAINED across an alloc cycle — for every <job> J and any sequence of intervening assigns of OTHER <job>s, once assign(J) returned F, a second assign(J) (the alloc-cycle case: stop -> new AllocationId -> new workload_addr but the SAME logical <job>) returns the SAME F (idempotent per <job>), and F is unchanged regardless of intervening assigns/releases of other <job>s. The atomic per-<job> claim follows development.md § 'Check-and-act must be atomic' (the assign return IS the claim). This is the SQ1-elimination contract at the allocator layer.",
            "S-DBN-FRONTEND-03 (withhold-not-release, Finding-2): a proptest in dns_frontend_allocator.rs proves the addr is RELEASED ONLY on logical-workload deletion, NEVER on a transient zero-healthy state — PROPERTY 1: given assign(J) returned F, while release(J) is NOT called (the transient zero-healthy window — the <job> is still declared, the allocator is never told to release), a subsequent assign(J) STILL returns the SAME F AND snapshot() still contains J -> F (the allocator carries NO health state). PROPERTY 2 (the genuine end): when release(J) IS called (logical-workload deletion/undeploy), snapshot() no longer contains J AND a later assign of a DIFFERENT <job> MAY draw F (the binding genuinely ended). Releasing F on a transient zero-healthy state would reintroduce the SQ1 stale-cached-F failure — the explicitly-rejected path.",
            "S-DBN-FRONTEND-04: a proptest in dns_frontend_allocator.rs proves collision-free distinct assignment — for every set of distinct <job> labels {J1..Jn} (n within the 10.98.0.0/16 block capacity), each Ji assign()ed yields pairwise-distinct {F1..Fn} (no collision — the rejected 1a-C deterministic-hash birthday-bound risk; 1a-A is an allocator precisely to avoid it), each Fi a member of 10.98.0.0/16, AND after release(Jk) a fresh assign of a NEW <job> MAY reuse Fk (the block is reclaimed; drop-then-reassign tracks the live set, the NetSlotAllocator converge-on-boot rebuild discipline).",
            "Pinned surface ONLY (CLAUDE.md 'Implement to the design'): pub struct FrontendAddrAllocator (a per-host <job> -> Ipv4Addr map, Arc<Mutex<BTreeMap>>, boot-rebuilt empty-on-boot per the NetSlotAllocator precedent) lands in crates/overdrive-control-plane/src/dns_responder/frontend_addr_allocator.rs with new() -> Self; assign(&self, job: &WorkloadId /* or MeshServiceName */) -> Result<Ipv4Addr, FrontendAddrExhausted> (idempotent per <job>, atomic check-and-act); release(&self, job) (logical-workload-DELETION only — NOT alloc cycle, NOT transient zero-healthy; zero-healthy is the name_index's WITHHOLD seam in 01-03, NOT a release here); snapshot() -> BTreeMap<job, Ipv4Addr>; const WORKLOAD_FRONTEND_BASE = 10.98.0.0/16 (Ipv4Net, lexically distinct from the existing ServiceFrontend VIP type at dataplane/service_frontend.rs). The exact slot-within-block arithmetic + ceiling is a DELIVER detail (mirror NetSlotAllocator's pure smallest-free scan); the block + the public surface above are the contract. Invent NO public surface beyond this. BLOCKER-1 RESOLVED (spike WORKS — the 10.98.0.0/16 block is routable+capturable through the shipped Path-A datapath); no dataplane gate remains on this step.",
            "RED convention: the frontend_addr_allocator.rs production stub lands RED via todo!(\"RED scaffold: …\") + the dns_responder module-scoped #![cfg_attr(not(test), expect(clippy::todo, …))]; the dns_frontend_allocator.rs test scaffolds land RED via #[should_panic(expected = \"RED scaffold\")]; RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate: a per-step diff-scoped run — cargo xtask lima run -- cargo xtask mutants --diff origin/main --features integration-tests --package overdrive-control-plane --file crates/overdrive-control-plane/src/dns_responder/frontend_addr_allocator.rs — achieves kill-rate >= 80%, targeting the pure smallest-free scan (a mutant that does not record the claim hands two <job>s the same F, flipping FRONTEND-04 pairwise-distinctness), the idempotent-assign branch (a mutant that re-allocates on the second assign flips FRONTEND-02), and the release path (a mutant that releases on a non-deletion signal flips FRONTEND-03)."
          ],
          "test_file": "crates/overdrive-control-plane/tests/acceptance/dns_frontend_allocator.rs",
          "scenario_name": "S-DBN-FRONTEND-01..04 — stable per-<job> frontend addr in 10.98.0.0/16, idempotent across cycles, release-only-on-deletion, collision-free"
        },
        {
          "id": "01-03",
          "name": "NameIndex (<job> -> stable frontend addr F, healthy gate as WITHHOLD seam, OQ-1 pinned) + pure answer_for",
          "criteria": [
            "S-DBN-ANSWER-01 (THE primary mutation-gate target, DDN-4; RE-SCOPED to stable F): a proptest in crates/overdrive-control-plane/tests/acceptance/dns_answer_for.rs proves that for every RESOLVABLE <job> name N (>=1 running-AND-healthy backend, so the name_index holds N -> F where F is N's STABLE frontend addr — REV-2: NOT a backend-addr set) plus arbitrary other resolvable names, answer_for(N, RecordType::A, &index) returns NameAnswer::Records(addrs) where addrs == vec![F] (exactly the single stable frontend addr, never a per-instance backend addr in 10.99.0.0/16). @example: N=server, F=10.98.x.y. Single-stable-F equality is the fail-closed guard.",
            "S-DBN-ANSWER-02 (RE-SCOPED): a proptest in dns_answer_for.rs proves that for every name N absent from the index OR present with 0 running-and-healthy backends, answer_for(N, RecordType::A, &index) returns NameAnswer::NxDomain and NEVER Records (the answer is WITHHELD — no stale/cached/guessed/frontend addr; declared-but-not-running, all-unhealthy, and unknown all collapse to NxDomain in v1).",
            "S-DBN-ANSWER-03 (RE-SCOPED): a proptest in dns_answer_for.rs proves answer_for(N, RecordType::AAAA, &index) returns NameAnswer::NoData for a resolvable name and NameAnswer::NxDomain for a withheld/absent name — never a fabricated v6 addr (pins the v1 contract table's AAAA column).",
            "S-DBN-ANSWER-04 (the load-bearing healthy gate as the WITHHOLD seam, REV-2 DDN-2/Finding-2): a proptest in crates/overdrive-control-plane/tests/acceptance/dns_name_index.rs builds a NameIndex via the List-then-Watch path from rows where Backend.healthy == false and proves answer_for(N, A) -> NxDomain for the unhealthy-only name N (the gate governs *resolvability* — withhold the answer), while a co-resident name M with a running-AND-healthy backend resolves to its stable F (Records == vec![F_M]) — proving the gate withheld only N, not the index. Asserts on answer_for return, NEVER the by_name BTreeMap internals.",
            "S-DBN-ANSWER-05 (PRESERVED): a single-example #[test] in dns_answer_for.rs proves answer_for('nonexistent.svc.overdrive.local', A, &index) -> NxDomain while 'server' in the same index still returns Records(vec![F]) (the miss does not corrupt the hit path).",
            "S-DBN-IDX-01 (RE-SCOPED to stable F): a proptest in dns_name_index.rs (driven by SimObservationStore) proves the index seeds from List at probe — a running-and-healthy ServiceBackendRow R whose Backend.alloc SVID job segment == N's <job>, with the FrontendAddrAllocator binding N -> F, makes N resolve to its STABLE frontend addr F (Records == vec![F], NOT R's per-instance Backend.addr); AND the watch half: an index probed against an empty store becomes resolvable for N to its F once a running-and-healthy row for N written AFTER probe is processed by the watch drain. Validates the OQ-1 mapping consumer (rows grouped by <job> to decide resolvability, then expose the allocator's stable F).",
            "S-DBN-IDX-02 (RE-SCOPED — withhold-not-release on the watch path, Finding-2): a proptest in dns_name_index.rs proves a name N initially resolvable to its stable F stops resolving (answer_for -> NxDomain) once a fresh row for the same backend with Backend.healthy == false is processed by the watch drain (the name_index WITHHOLDS), AND the FrontendAddrAllocator STILL binds N -> the SAME F (snapshot() still contains N -> F — withhold-not-release; the stable F is retained across the zero-healthy window), AND the moment a running-AND-healthy row for N is written again, N resolves to the SAME F (no churn of the addr). A mutant that releases F on zero-healthy passes the NXDOMAIN check but fails the retained-F check.",
            "S-DBN-IDX-03 (PRESERVED, addr-agnostic): a proptest in dns_name_index.rs proves a SubscriptionEvent::Lagged { missed } triggers a re-List via all_service_backends_rows(), after which the index reflects the store state S exactly (every <job> with a running-and-healthy row in S resolves to its stable F, no <job> absent from S resolves) — the relist-on-Lagged recovery the ServiceBackendsResolve sibling enforces (re-derives resolvability from rows regardless of whether the exposed value is a backend addr or a stable F).",
            "S-DBN-IDX-04 (RE-SCOPED — single source of frontend truth = the FrontendAddrAllocator, the SAME instance 02-00's by_frontend reads): a proptest in dns_name_index.rs proves the answered F is the FrontendAddrAllocator's binding and the index introduces no second source of frontend truth — for any resolvable N, answer_for(N, A, &index) returns Records(vec![the FrontendAddrAllocator's F for N's <job>]) (no fabricated addr, no cached snapshot that outlived the allocator state), AND removing all rows for N from S and re-deriving makes N resolve to NxDomain (withheld — no stale retention at the index) WHILE the FrontendAddrAllocator still binds N -> F (the binding is the allocator's, not the index's). The single source named here is byte-identical to the one 02-00's by_frontend re-key derives F from (the SAME FrontendAddrAllocator instance, injected once by run_server per 02-01 / ADR-0072 DDN-2) — so the answered F == the recognized F and neither 01-03 nor 02-00 may introduce a second <job> -> F source. The in-memory half of the K-DBN-4 single-source oracle (REV-2 form: two single-source claims — frontend truth = the FrontendAddrAllocator, liveness truth = the service_backends rows; the index fabricates neither).",
            "OQ-1 STILL OPEN under REV-2 (CLAUDE.md 'Implement to the design — never invent API surface'): the crafter MUST surface OQ-1 — the SpiffeId -> <job> accessor, CONFIRMED-ABSENT today (SpiffeId exposes only as_str/trust_domain/path + for_allocation, id.rs:269-304) — needed now to key the FrontendAddrAllocator by <job> AND group running-and-healthy rows under it; pin the exact accessor signature (a new SpiffeId::job_segment() -> Option<&str> on the newtype OR a parse helper local to name_index.rs) as a DECISION before improvising; if the shape is ambiguous, return a BLOCKER. DISTILL picks no signature; the crafter must not reach for the nearest mechanism that compiles. (The FrontendKey newtype-vs-tuple shape and the CIDR-membership accessor are 02-00 DELIVER-open details, not this step.)",
            "Pinned surface ONLY: pub fn answer_for(name: &MeshServiceName, qtype: hickory_proto::rr::RecordType, index: &NameIndex) -> NameAnswer (signature UNCHANGED; Records now holds vec![F], one stable frontend addr per <job>) lands in crates/overdrive-control-plane/src/dns_responder/answer.rs; NameIndex (maps <job> -> stable frontend addr F, NOT -> a backend-addr set) with List-then-Watch + relist-on-Lagged + single-owner-drain + probe() (mirroring ServiceBackendsResolve at mtls_resolve_adapter.rs:307-471), the mandatory Backend.healthy == true gate as the WITHHOLD seam (REV-2 DDN-2/Finding-2 — governs resolvability, not which addr), and consuming the FrontendAddrAllocator <job> -> F binding from the SAME FrontendAddrAllocator instance 02-00's by_frontend re-key reads (the new dependency on 01-04; the single source of frontend truth, ADR-0072 DDN-2 single-owner invariant — run_server injects one instance into both per 02-01), lands in crates/overdrive-control-plane/src/dns_responder/name_index.rs. The name_index MUST NOT introduce a second <job> -> F source (no fabricated addr, no cached snapshot) — the answered F is the allocator's binding, byte-identical to the F by_frontend recognizes. Invent NO public surface beyond this; the internal by_name structure is a DELIVER detail asserted only through answer_for.",
            "RED convention: production stubs via todo!(\"RED scaffold: …\") + the dns_responder module-scoped expect(clippy::todo); dns_answer_for.rs + dns_name_index.rs scaffolds via #[should_panic(expected = \"RED scaffold\")]; RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate (answer_for is THE primary kill target, DDN-4): a per-step diff-scoped run — cargo xtask lima run -- cargo xtask mutants --diff origin/main --features integration-tests --package overdrive-control-plane --file crates/overdrive-control-plane/src/dns_responder/answer.rs --file crates/overdrive-control-plane/src/dns_responder/name_index.rs — achieves kill-rate >= 80%, targeting answer_for's Records(vec![F])/NxDomain/NoData arms (an empty/withheld/wrong-name mutant flips the single-stable-F equality) and the NameIndex healthy-gate WITHHOLD seam (a mutant dropping the filter resolves a should-be-withheld <job>; a mutant releasing F on zero-healthy flips the retained-F check IDX-02)."
          ],
          "test_file": "crates/overdrive-control-plane/tests/acceptance/dns_answer_for.rs",
          "scenario_name": "S-DBN-ANSWER-01..05 + S-DBN-IDX-01..04 — pure answer_for contract (Records=vec![F]) + NameIndex <job>->stable F List-then-Watch / healthy-gate WITHHOLD seam (OQ-1 pinned)"
        }
      ]
    },
    {
      "id": "02",
      "name": "The responder + the walking-skeleton vertical slice (US-DBN-2)",
      "steps": [
        {
          "id": "02-00",
          "name": "MtlsResolve re-key — by_frontend (FrontendKey=(SocketAddrV4,Proto)) translation + three-way classify + ordered drain (1b-A)",
          "criteria": [
            "S-DBN-REKEY-01 (the frontend->live-backend translation, K-DBN-4): a proptest in crates/overdrive-control-plane/tests/acceptance/mtls_resolve_rekey.rs proves that for every ServiceId Svc with >=1 running-AND-healthy backend, a frontend key K = (F, listener.port, Proto::Tcp) bound by_frontend[K] == Svc (and arbitrary by_addr entries), classify(orig_dst=(F, listener.port), proto=Tcp) returns Mesh AND the selected backend is the FIRST-by-Ord running-AND-healthy backend for Svc (BLOCKER-2 pinned: deterministic first-by-Ord — v1 single-replica is degenerate, but the tie-break keeps DST replay-equivalence and is mutation-gate-able) AND it is NEVER NonMesh (a frontend HIT is always mesh) and NEVER an unhealthy backend. @example: Svc=server, K=(10.98.x.y, 8080, Tcp), one healthy backend -> Mesh(that backend). The Cilium ClusterIP->backend translation made executable (research SQ4).",
            "S-DBN-REKEY-02 (frontend hit but zero-healthy -> MeshUnreachable): a proptest in mtls_resolve_rekey.rs RELAXES REKEY-01's '>=1 healthy backend' precondition — for every Svc bound by_frontend[K]==Svc (K=(F,port,Tcp)) whose backend set is currently EMPTY or all-unhealthy, classify((F,port),Tcp) returns MeshUnreachable (the service is KNOWN — the key matched — but has no healthy backend right now: fail-closed, refuse, NO cleartext) AND NEVER Mesh AND NEVER NonMesh. Distinct from FAILCLOSED-01 (a frontend-subnet MISS); here the key HITS but the healthy set is empty.",
            "S-DBN-REKEY-03 (the proto axis is a key field, Finding-1): a proptest in mtls_resolve_rekey.rs proves the frontend key discriminates proto — for one frontend IP F, one port P, and two DISTINCT services Svc_tcp and Svc_udp whose listeners are (F,P,Tcp) and (F,P,Udp), by_frontend holding (F,P,Tcp)->Svc_tcp AND (F,P,Udp)->Svc_udp yields Svc_tcp for key (F,P,Tcp) and Svc_udp for (F,P,Udp) (one frontend IP fronting N distinct (port,proto) listeners = N distinct entries), and a bare SocketAddrV4 (F,P) WITHOUT the proto axis CANNOT distinguish them (the collision Finding-1 names — the key MUST carry proto). The production row-producing path is proto-aware/UDP-admissible (service_map_hydrator.rs:130 + the C3 guard refusing to default to Tcp); v1 captures TCP at the worker layer so the live resolve call site keys Proto::Tcp — this Tier-1 property exercises the index's proto-discrimination at the data-structure level. A mutant that drops the Proto field collapses the two services onto one key.",
            "S-DBN-REKEY-04 (backward-compatible additive EXTEND): a proptest in mtls_resolve_rekey.rs proves a direct backend-addr dial still resolves via by_addr — for every running-and-healthy backend addr B (in 10.99.0.0/16, a per-instance workload addr, NOT a frontend addr) indexed in by_addr (and NOT in by_frontend), classify(orig_dst=B, proto) returns the SAME verdict the pre-REV-2 by_addr path returns (Mesh for healthy B; MeshUnreachable for unhealthy B), AND a true non-mesh dst (not in by_frontend, not in by_addr, not in 10.98.0.0/16) still returns NonMesh (legitimate non-mesh egress, unchanged). The backward-compatibility guard: a mutant that routes a by_addr hit through the frontend path, or breaks the non-mesh fall-through, flips this — proving the extension did not regress the security-critical existing path.",
            "S-DBN-FAILCLOSED-01 (the structural defense, Finding-3; K-DBN-4): a proptest in mtls_resolve_rekey.rs proves the THREE-way classify's fail-closed-on-frontend-subnet-miss arm. PROPERTY 1: for every orig_dst whose ip is a member of 10.98.0.0/16 that MISSES by_frontend AND MISSES by_addr, classify(orig_dst, proto) returns MeshUnreachable (refuse, NO cleartext — a mesh dial that arrived before the index was ready (race) OR to a withdrawn <job>) AND is NEVER NonMesh (NEVER cleartext PassThrough — the fail-OPEN regression the subnet-scoped arm exists to prevent). PROPERTY 2: for every orig_dst whose ip is NOT a member of 10.98.0.0/16 that misses both maps, classify returns NonMesh (legitimate non-mesh egress -> cleartext, by design — the live rustdoc requires a GENERAL miss stay NonMesh, mtls_resolve_adapter.rs:128-130). The two properties together are load-bearing: a mutant treating the subnet miss as NonMesh flips Property 1 (the fail-open footgun); a mutant treating EVERY miss as MeshUnreachable flips Property 2 (breaks legitimate non-mesh egress). Membership-check primitive is a DELIVER detail (the orig_dst.ip() in 10.98.0.0/16 CIDR-containment accessor — an Ipv4Net::contains on the const or a mask compare; the crafter MUST NOT invent a broader 'is this any reserved subnet' helper, REV-2 § Frontend-subnet coherence contract).",
            "S-DBN-COHERENCE-01 (the write-time ordering barrier, Finding-3 (i)): a proptest in crates/overdrive-control-plane/tests/acceptance/dns_name_index.rs (the drain feeds both projections) proves that for every batch of running-and-healthy rows that binds <job> -> F applied through the single ordered drain, there is NO observable moment where name_index answers F for <job> while by_frontend does NOT yet hold the (F, listener.port, proto) entry (the ordering invariant: by_frontend is updated FIRST, name_index exposes F SECOND — option (b), the write-time barrier) AND therefore DNS never answers an F that the re-keyed resolve has not already learned (a steady-state-race-free coherence guarantee). The F bound into by_frontend and the F exposed to name_index are byte-identical because the ordered drain derives BOTH from the SAME FrontendAddrAllocator instance (01-04) — the single source of frontend truth (ADR-0072 DDN-2 single-owner invariant); the service_backends rows supply liveness, the allocator supplies which F. Both projections derive from ONE ordered apply over the same service_backends rows (the 'one source, three readers' model, mtls_resolve_adapter.rs:56-87) reading the ONE allocator binding. A mutant that exposes F to name_index before binding by_frontend flips this ordering property.",
            "S-DBN-EQUIV-01 (DST equivalence — the trait-docstring contract guard): a proptest in mtls_resolve_rekey.rs proves host and in-memory BackendIndex/MtlsResolve agree on the re-keyed classify over the SAME call sequence — for every sequence of (orig_dst, proto) classify calls and every sequence of row applies/withdrawals, two BackendIndex instances driven through the SAME ordered sequence of row applies + frontend bindings (the host build path and the in-memory test-double path honouring the same contract) return the byte-identical verdict (Mesh{backend} / MeshUnreachable / NonMesh) at EVERY step AND the verdict trajectory is deterministic (seed -> bit-identical, the DST replay-equivalence property — the first-by-Ord selection rule is what makes this hold across builds). Mandated by development.md § 'Trait definitions specify behavior': the three-way arm + the first-by-Ord selection is the contract, the equivalence test is the enforcement. Exercises the BackendIndex/classify data structure in-process at Tier 1 (no real socket — the socket is irreducibly Tier 3, DDN-4).",
            "Pinned surface ONLY (CLAUDE.md 'Implement to the design'): EXTEND BackendIndex (additive; mtls_resolve_adapter.rs:207, the existing by_addr/addrs_by_service maps UNCHANGED) with by_frontend: BTreeMap<FrontendKey, ServiceId> where FrontendKey = (SocketAddrV4, Proto) = (F, listener.port, listener.protocol) — the ServiceId VALUE is the row's existing content-addressed ServiceId (ServiceBackendRow.service_id), NOT a re-derivation. FrontendKey MAY be a named newtype (DELIVER detail); the CONTRACT is that the key discriminates proto. The additive classify is a THREE-way branch: (1) by_frontend HIT -> ServiceId -> first-by-Ord running-AND-healthy backend -> Mesh (else MeshUnreachable); (2) MISS but orig_dst.ip() in 10.98.0.0/16 -> MeshUnreachable (fail-closed-on-frontend-subnet-miss, NO cleartext); (3) MISS outside the subnet -> fall through to today's by_addr lookup. A SINGLE ordered drain feeds both by_frontend and name_index, updating by_frontend BEFORE name_index exposes F (option (b)), and derives the <job> -> F binding for by_frontend's key from the SAME FrontendAddrAllocator instance (01-04) the 01-03 name_index answers F from — the single source of frontend truth (ADR-0072 DDN-2 single-owner invariant; run_server injects ONE instance into both per 02-01). by_frontend MUST NOT introduce a second <job> -> F source: the F it keys on is the allocator's binding, byte-identical to the F the DNS path answers, which is what makes an answered F always HIT by_frontend (never miss -> fail-closed). The re-keyed MtlsResolve trait-docstring contract MUST pin all three: the (F, listener.port, Proto) key type, the first-by-Ord selection rule, AND the fail-closed-on-frontend-subnet-miss arm + the equivalence test (development.md § 'Trait definitions specify behavior'). Edits mtls_resolve_adapter.rs — REV-1's DDN-1 'intercept struct provably untouched' is SUPERSEDED (the struct is extended additively, user-ratified by choosing the thin path). BLOCKER-2 RESOLVED (deterministic first-by-Ord). Invent NO surface beyond this; the v1 resolve call site keys Proto::Tcp because the worker-layer capture is TCP (mtls_intercept_worker.rs:792-794) — the future-UDP capture is the named plumbing step, NOT an index-key change. TWO REV-2 DELIVER-open details: the FrontendKey newtype-vs-tuple shape and the CIDR-membership accessor (an Ipv4Net::contains on WORKLOAD_FRONTEND_BASE) — pin as a DECISION, do not improvise.",
            "RED convention: the mtls_resolve_adapter.rs by_frontend/classify production EXTEND lands RED via todo!(\"RED scaffold: …\") on the new arms + the appropriate expect(clippy::todo); mtls_resolve_rekey.rs + the dns_name_index.rs COHERENCE-01 scaffold land RED via #[should_panic(expected = \"RED scaffold\")]; RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate (the re-key is a primary kill target): a per-step diff-scoped run — cargo xtask lima run -- cargo xtask mutants --diff origin/main --features integration-tests --package overdrive-control-plane --file crates/overdrive-control-plane/src/mtls_resolve_adapter.rs — achieves kill-rate >= 80%, targeting the three-way classify arms (a mutant flattening the fail-closed-on-subnet-miss arm to NonMesh flips FAILCLOSED-01 Property 1; a mutant picking last-by-Ord or an unhealthy backend flips REKEY-01; a mutant dropping the Proto from FrontendKey flips REKEY-03), the first-by-Ord selection rule, and the ordered-drain barrier (a mutant reordering the projection apply flips COHERENCE-01)."
          ],
          "test_file": "crates/overdrive-control-plane/tests/acceptance/mtls_resolve_rekey.rs",
          "scenario_name": "S-DBN-REKEY-01..04 + S-DBN-FAILCLOSED-01 + S-DBN-COHERENCE-01 + S-DBN-EQUIV-01 — by_frontend translation, three-way fail-closed classify, ordered drain, DST equivalence"
        },
        {
          "id": "02-01",
          "name": "DnsResponder host adapter (IP_PKTINFO socket loop, wildcard->per-addr fallback) + FrontendAddrAllocator + re-keyed MtlsResolve wired into run_server",
          "criteria": [
            "S-DBN-BIND-01: a Tier-3 #[tokio::test] in crates/overdrive-control-plane/tests/integration/dns_responder_bind.rs (gated #![cfg(feature=\"integration-tests\")], run via cargo xtask lima run -- cargo nextest run --features integration-tests, NEVER --no-run; is_root() SKIP per K1, uname -r recorded) proves DnsResponder::probe binds 0.0.0.0:53 (SO_REUSEADDR + IP_PKTINFO) coexisting with systemd-resolved's 127.0.0.53:53/127.0.0.54:53 specific binds (ss -ulnp confirms no EADDRINUSE), and getent from EACH of >=2 provisioned netns resolves its query — each reply source-pinned to the queried gateway via ipi_spec_dst. The acceptance SIGNAL is getent/getaddrinfo, NEVER dig @gw alone (the spike litmus: dig passes a missing source-pin, getent does not).",
            "S-DBN-BIND-02: a Tier-3 #[tokio::test] in dns_responder_bind.rs forces a wildcard 0.0.0.0:53 EADDRINUSE via a stand-in wildcard holder and proves the per-gateway-addr fallback re-derives the bound socket set from net_slot_allocator.snapshot() — one :53 socket per assigned gateway (desired == { responder_addr_for_slot(slot) : slot in snapshot }), getent from each netns resolves, AND on the converge tick a newly-assigned slot binds a new per-addr socket (add-if-missing) and a released slot drops its socket (drop-if-absent), the bound set tracking the live slot set (reconcilers.md Bar-1 converge). Example-based sad path (Mandate 11). DELIVER MAY split the fallback-fires and the converge-tracks assertions into two #[test] bodies (reviewer suggestion #2).",
            "S-DBN-BIND-03: a Tier-3 #[tokio::test] in dns_responder_bind.rs proves the Earned-Trust gate (DDN-6, wire->probe->use): Scenario A (no bindable :53 — wildcard AND every per-gateway-addr held) -> probe returns Err(DnsResponderError::Bind { addr, source }), run_server_with_obs_and_driver REFUSES boot (returns an error, process exits non-zero), and a structured health.startup.refused event names the bind failure; Scenario B (all_service_backends_rows() fails at probe) -> probe returns Err(DnsResponderError::ListSeed { reason }) and boot REFUSES with a List-failure reason. Mirrors MtlsResolve.probe() -> health.startup.refused -> MtlsBoot (lib.rs ~1904).",
            "Pinned surface ONLY: impl DnsResponder { new(store: Arc<dyn ObservationStore>, clock: Arc<dyn Clock>, slots: veth_provisioner::NetSlotAllocator, frontend: FrontendAddrAllocator) -> Self; async fn probe(&self) -> Result<(), DnsResponderError>; async fn serve(self: Arc<Self>) } lands in crates/overdrive-control-plane/src/dns_responder/responder.rs (wildcard-first then per-addr fallback, the recvmsg/sendmsg IP_PKTINFO loop with ipi_spec_dst source-pin). The fourth param `frontend: FrontendAddrAllocator` is the single `<job> -> F` owner (01-04) the name_index answers F from — DISTINCT from `slots` (the per-netns reply-source-pin / per-addr fallback allocator, DDN-5, a DIFFERENT concern: keep BOTH). It is the SAME FrontendAddrAllocator instance injected into the re-keyed MtlsResolve (02-00), so the answered F is byte-identical to the F by_frontend recognizes (ADR-0072 DDN-2 single-owner invariant); the responder MUST NOT construct its own allocator or derive F independently. enum DnsResponderError { Bind { addr: SocketAddr, source: std::io::Error }, ListSeed { reason: String }, Probe { reason: String }, Socket { source: std::io::Error } } — typed thiserror, NO Internal(String), each variant -> a distinct health.startup.refused reason (development.md § 'Never flatten a typed error to Internal(String)'). Invent no surface beyond this.",
            "Production wiring IS this step (DDN-6, no deferral; REV-2 EXTEND): EXTEND run_server_with_obs_and_driver (crates/overdrive-control-plane/src/lib.rs ~1893-1957) to construct exactly ONE FrontendAddrAllocator and inject the SAME Arc-shared handle into BOTH paths — the single-owner invariant (ADR-0072 DDN-2) that makes the F the DNS path answers byte-identical to the F the resolve path recognizes. Concretely: (a) construct the single FrontendAddrAllocator (01-04) and share it on AppState beside the existing net_slot_allocator; (b) construct the re-keyed MtlsResolve (02-00) GIVEN that SAME FrontendAddrAllocator handle so its single ordered drain derives the <job> -> F binding from it (by_frontend recognizes that F) — the probed-Ok resolve is then moved into the worker, so the worker-held resolve and the responder share the one allocator; (c) construct the DnsResponder AFTER resolve.probe() with store + config.clock + the state.net_slot_allocator handle (the DDN-5 per-addr fallback source) + the SAME FrontendAddrAllocator handle (so the name_index answers the allocator-owned F — NOT a second source), call responder.probe() (binds + seeds the name_index via List), refuse boot with health.startup.refused on bind/List failure, tokio::spawn the serve loop, and hold the JoinHandle — all gated by the SAME if mtls_worker.is_some() real-dataplane block that gates the netns/intercept path. The shipped construction site supports this: resolve is an Arc<dyn MtlsResolve> (lib.rs:1910) moved into the worker (:1937) and the allocator is Arc-shared, so one `let frontend = FrontendAddrAllocator::new()` binding can feed the resolve before the move AND survive (held on AppState) to the responder construction after AppState::new (:1944). ADD the nix socket/uio features (recvmsg/sendmsg/ControlMessage::Ipv4PacketInfo) to overdrive-control-plane + workspace nix features. The socket loop / bind / IP_PKTINFO / Earned-Trust gate are UNCHANGED from REV-1; the composition-root delta is the single FrontendAddrAllocator + its injection into BOTH the re-keyed resolve and the responder. Depends on 01-03 (name_index), 01-04 (FrontendAddrAllocator), 02-00 (re-keyed MtlsResolve).",
            "No Tier-2 (DDN-4): the IP_PKTINFO multi-homed socket has no BPF_PROG_TEST_RUN backstop; the DST seam is the pure answer_for (01-03) + the proptested wire.rs (01-02). The socket loop is irreducibly Tier-3 real-kernel; a green check or --no-run proves nothing about runtime source-pinning.",
            "RED convention: responder.rs production stubs via todo!(\"RED scaffold: …\") + the dns_responder module expect(clippy::todo); Tier-3 scaffolds via #[should_panic(expected = \"RED scaffold\")] (or #[ignore] only if blocked on an external resource per .claude/rules/testing.md); RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate: a per-step diff-scoped run — cargo xtask lima run -- cargo xtask mutants --diff origin/main --features integration-tests --package overdrive-control-plane --file crates/overdrive-control-plane/src/dns_responder/responder.rs (and the lib.rs composition diff) — achieves kill-rate >= 80%, targeting the DnsResponderError variant -> refusal-reason mapping (a mutant flattening to one reason flips BIND-03) and the run_server construct+probe+spawn (deleting the spawn RED's the walking skeleton). The Tier-3 socket arms (IP_PKTINFO, ipi_spec_dst, fallback re-derive) are killed by the Tier-3 getent litmus, not in-process mutants."
          ],
          "test_file": "crates/overdrive-control-plane/tests/integration/dns_responder_bind.rs",
          "scenario_name": "S-DBN-BIND-01/02/03 — wildcard IP_PKTINFO bind + source-pin, per-addr fallback converge, Earned-Trust refuse-boot"
        },
        {
          "id": "02-02",
          "name": "Walking skeleton — getent <job>.svc.overdrive.local resolves to a STABLE frontend F, F translates to the live backend, the hop is mTLS'd, F is stable across alloc cycles, and in-flight churn fails fast",
          "criteria": [
            "S-DBN-WS (the production-drivable loop proof, US-DBN-2 · K-DBN-1; RE-DISTILLed to stable F): a Tier-3 #[tokio::test] in crates/overdrive-control-plane/tests/integration/dns_responder_walking_skeleton.rs (gated #![cfg(feature=\"integration-tests\")], run via cargo xtask lima run -- cargo nextest run --features integration-tests, NEVER --no-run; is_root() SKIP per K1, uname -r recorded; mirrors canonical_address_inbound_walking_skeleton.rs boot/deploy/netns shape) proves: GIVEN run_server_with_obs_and_driver booted in-process (real EbpfDataplane, mtls_identity_override = Some(TestPki), so mtls_worker.is_some() is active and the DnsResponder + FrontendAddrAllocator + re-keyed MtlsResolve are constructed+probed+spawned per DDN-6 + REV-2 02-00/02-01) AND a 'server' Service deployed through POST /v1/jobs reached Running-AND-HEALTHY so its <job> 'server' is bound a STABLE frontend addr F (a member of 10.98.0.0/16) and exposed by name_index, WHEN a client workload resolves 'server.svc.overdrive.local' via getaddrinfo/getent (NOT dig — K2) from inside its netns and connects to F:SERVICE_PORT, THEN getent resolves to the STABLE frontend addr F (a member of 10.98.0.0/16, NOT a per-instance backend addr in 10.99.0.0/16) within the K2 5s budget AND the connection to (F, SERVICE_PORT) is captured by the PRODUCTION-installed egress nft-TPROXY rule (BLOCKER-1 WORKS — destination-blind capture recovers orig_dst=(F,SERVICE_PORT) verbatim), the re-keyed MtlsResolve translates (F, SERVICE_PORT, Tcp) -> the current live backend, mTLS terminates on the leg, and the peer wire carries TLS 1.3 application_data records (0x17) with ZERO payload cleartext (the keystone's transitive round-trip litmus). Assert getent resolution (to F) first and separately from the mTLS assertion (reviewer suggestion #1, keeping the K2 two-culprits message honest).",
            "S-DBN-WS-STABLE (THE SQ1-elimination AC, US-DBN-2 · K-DBN-1; NEW): a Tier-3 #[tokio::test] in dns_responder_walking_skeleton.rs sharing the S-DBN-WS boot fixture proves the answered F is BYTE-STABLE across an alloc cycle — GIVEN getent 'server.svc.overdrive.local' resolves to stable frontend F1 (recorded) and one connect to F1 lands backend B1, WHEN the server backend is CYCLED (stopped — its AllocationId ends, its per-instance workload_addr freed — and a NEW instance starts with a NEW AllocationId -> NEW workload_addr B2, the <job> 'server' still declared) and reaches Running-AND-HEALTHY and getent is re-queried, THEN getent resolves to the SAME F1 byte-for-byte (the FrontendAddrAllocator's idempotent assign('server') returns the same F — Finding-2) AND the subsequent connect to (F1, SERVICE_PORT) lands the NEW backend B2 (the re-keyed MtlsResolve re-resolved the live backend per-connect, the churn half mtls_resolve_adapter.rs:521-540 REUSED), mTLS terminates, peer wire carries 0x17 AND at NO point did getent return a per-instance backend addr (neither B1 nor B2 in 10.99.0.0/16 was ever the resolved value — only the stable F1). One Tier-3 alloc-cycle golden example (Mandate 11); the PBT half of stability is S-DBN-FRONTEND-02 at Tier 1.",
            "S-DBN-SINGLE-SRC (the byte-consistency oracle, US-DBN-2 · K-DBN-4; RE-DISTILLed to the frontend->backend translation invariant D-DBN-6): a Tier-3 #[tokio::test] in dns_responder_walking_skeleton.rs sharing the S-DBN-WS boot fixture proves that the addr F resolved via getaddrinfo, fed (as a SocketAddrV4 at SERVICE_PORT) into the production re-keyed MtlsResolve.resolve(F, Tcp) for the same flow, recognizes F (a by_frontend HIT — NOT a miss -> NonMesh) AND classifies it Mesh (NOT MeshUnreachable, NOT NonMesh) AND TRANSLATES F to a current running-AND-healthy backend (the Mesh verdict carries the live backend the connection mTLS-originates to) AND F is byte-identical to the addr the responder answered (one source: the name answer and the resolve translation read the SAME <job> -> F binding from the SAME FrontendAddrAllocator; the translation lands a healthy backend from the SAME service_backends rows — the REV-2 byte-consistency restatement). The intercept struct (mtls_resolve_adapter.rs) is EXTENDED additively (REV-2 supersedes REV-1's 'untouched'), read for the oracle.",
            "S-DBN-CHURN (the active-termination posture, US-DBN-4 · K-DBN-2; NEW — NO sock_destroy): a Tier-3 #[tokio::test] in dns_responder_walking_skeleton.rs (or a churn-named file) sharing the boot fixture proves in-flight churn fails fast — GIVEN a client holding an OPEN in-flight connection through the intercept to the current backend B1 (data flowing), WHEN B1 is CYCLED mid-connection, THEN the client's in-flight connection gets a PROMPT reset/error bounded by TCP_USER_TIMEOUT (a connection-reset or timeout surfaced within the TCP_USER_TIMEOUT window) — NOT an indefinite hang AND NO sock_destroy is used (backend death surfaces through the per-connection pump task + TCP_USER_TIMEOUT/keepalive on the worker proxy legs, mtls_intercept_worker.rs:34 — the terminating-proxy posture; sock_destroy is #61 scope, NOT here) AND TCP_USER_TIMEOUT is confirmed tuned to a sane bound (the in-flight dial fails FAST, the way Cilium's own default leaves TCP socket-termination OFF, research SQ5.3) AND a SUBSEQUENT fresh connect to F lands the new live backend B2. Distinct from S-DBN-WS-STABLE: that proves the NEXT dial is live; this proves the IN-FLIGHT dial fails fast rather than hangs. One named Tier-3 example (Mandate 11).",
            "Vertical-slice litmus (CLAUDE.md 'Build vertical slices through production entry points'): removing the production responder spawn in run_server (DDN-6) takes S-DBN-WS RED — getent times out (nothing answers on the gateway) and the dial never starts; removing the by_frontend translation arm (02-00) takes S-DBN-WS RED too — the connect to F misses by_frontend -> fail-closed (MeshUnreachable) -> no backend reached. NO test binds :53, installs a resolv.conf, allocates F, programs a map, or hand-installs the egress capture — production does all of those itself (the capture via the shipped Path-A datapath, BLOCKER-1 WORKS). The dial-by-name addition over the canonical-address keystone is ONLY the getaddrinfo/getent resolution STEP (turning a name into the stable F) PLUS the re-keyed MtlsResolve translating F -> a live backend; the capture + handshake + mTLS round-trip datapath is REUSED verbatim from the proven keystone (the intercept LANDING + the 0x17 round-trip is itself the proof the resolved F was translated to the right backend — a wrong F would miss by_frontend and fail-close, never reach a backend).",
            "getent-not-dig (the spike litmus, K2): the acceptance signal is getent ahosts/hosts <name> (or a real getaddrinfo() call) run from inside the deployed client workload's netns; a scenario asserting on dig @gw alone would PASS a broken responder (missing ipi_spec_dst source-pin) — forbidden. The K2 failure message names the two equally-likely culprits (source-pin OR healthy-gate regression).",
            "Merge gate (ADR-0068): the Tier-3 walking-skeleton / stable-F / single-source / churn ACs assert the pinned-6.18 appliance-kernel Tier-3 matrix; dev-Lima 7.0.0-22-generic (the spike kernel) is necessary-but-not-sufficient — the IP_PKTINFO / multi-homed UDP / per-netns resolv.conf / SO_REUSEADDR-wildcard-coexistence / frontend-subnet egress-capture / TCP_USER_TIMEOUT surfaces are long-stable but MUST be re-confirmed on 6.18 (a DEVOPS/Tier-3 obligation, ADR-0072 § Design constraints inherited from the spike).",
            "Pinned surface ONLY: this step adds NO new production type — it drives DnsResponder::{new, probe, serve} + the run_server composition (02-01), the FrontendAddrAllocator (01-04), and the re-keyed MtlsResolve (02-00) end-to-end through the real serve+deploy path. The client program (getaddrinfo+connect) is a staged tiny Rust bin OR a /usr/bin/python3 -c one-liner pointing at a REAL on-disk binary (K3, no phantom paths — the dns-resolver.toml collision class); listener ports avoid 5353 (systemd-resolved) and 53 (the responder).",
            "RED convention: Tier-3 scaffolds in dns_responder_walking_skeleton.rs land via #[should_panic(expected = \"RED scaffold\")] (or #[ignore] only if blocked on the hickory dep / a real BPF artifact per .claude/rules/testing.md); RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate: this step lands Tier-3 acceptance tests over already-mutation-gated production logic (answer_for 01-03, wire.rs 01-02, FrontendAddrAllocator 01-04, the re-keyed classify 02-00, responder.rs/lib.rs 02-01) and adds no new mutable in-process logic of its own (the TCP_USER_TIMEOUT pump-task surfacing is REUSED production exercised by S-DBN-CHURN at Tier 3, not new in-process logic); the per-PR per-package diff run (cargo xtask lima run -- cargo xtask mutants --diff origin/main --features integration-tests --package overdrive-control-plane) is the gate. If this step touches no new mutable source, the per-step --file run has nothing to mutate (a vacuous pass) — the final per-PR run is the backstop."
          ],
          "test_file": "crates/overdrive-control-plane/tests/integration/dns_responder_walking_skeleton.rs",
          "scenario_name": "S-DBN-WS + S-DBN-WS-STABLE + S-DBN-SINGLE-SRC + S-DBN-CHURN — deployed workload resolves its peer's STABLE frontend name through serve+deploy, F translates to the live backend, the hop is mTLS'd, F is byte-stable across an alloc cycle, in-flight churn fails fast"
        }
      ]
    },
    {
      "id": "03",
      "name": "Outward slices (empty-candidate honesty + bidirectional demo)",
      "steps": [
        {
          "id": "03-01",
          "name": "Empty-candidate honesty / NXDOMAIN end-to-end (US-DBN-4)",
          "criteria": [
            "S-DBN-NXDOMAIN-01 (US-DBN-4 · K-DBN-2; RE-DISTILLed — withhold then resolve-to-stable-F): a Tier-3 #[tokio::test] in crates/overdrive-control-plane/tests/integration/dns_responder_nxdomain.rs (gated #![cfg(feature=\"integration-tests\")], run via cargo xtask lima run -- cargo nextest run --features integration-tests, NEVER --no-run; is_root() SKIP per K1) proves that with a 'server' Service deployed through POST /v1/jobs but NOT yet Running-AND-HEALTHY (Pending OR Running-but-unhealthy), a deployed client querying 'server.svc.overdrive.local' via getaddrinfo/getent (NOT dig — K2) from inside its netns gets name-does-not-resolve (NXDOMAIN — the name_index WITHHOLDS the answer, no running-and-healthy backend), NO address returned (no stale/cached/unhealthy/guessed addr — NOT a stable frontend F either, since F is withheld until the <job> has a running-and-healthy backend), AND once the server reaches Running-AND-HEALTHY a re-query resolves to the stable frontend addr F (a member of 10.98.0.0/16) (the 1s SOA negative-TTL lets the retry land promptly — DDN-8). Example-based sad path (Mandate 11).",
            "S-DBN-NXDOMAIN-02 (US-DBN-4 · K-DBN-2; RE-DISTILLed — withhold-not-release, Finding-2): a Tier-3 #[tokio::test] in dns_responder_nxdomain.rs proves that after a Running-AND-HEALTHY 'server' (resolving to its stable frontend addr F via getent) is stopped through the production stop path (POST /v1/jobs/server/stop, the run_server_stop precedent at keystone:635) and converges to Terminated (poll the obs row, per the keystone's server_stopped), leaving the <job> 'server' zero-healthy but STILL DECLARED (not deleted), a deployed client re-querying 'server.svc.overdrive.local' via getent gets NXDOMAIN (the name_index WITHHELD the answer — zero running-and-healthy backends) AND NEVER the stale F's translated backend nor any stale addr (the index dropped the resolvability; no second source of liveness truth), AND (withhold-not-release): re-deploying/recovering the SAME <job> 'server' to Running-AND-HEALTHY makes getent resolve to the SAME F as before the stop (the FrontendAddrAllocator RETAINED F across the zero-healthy window — F is per-logical-workload, released ONLY on logical-workload deletion; releasing F on the transient stop would reintroduce the SQ1 stale-cached-F failure, explicitly rejected). The Tier-3 observable of the S-DBN-IDX-02 / S-DBN-FRONTEND-03 Tier-1 withhold-not-release contract. Chained narrative reusing S-DBN-WS's Given+When (one name's lifecycle). (A withdrawn/DELETED <job> would also NXDOMAIN AND release its F via the allocator's release — the deletion case is S-DBN-FRONTEND-03 Property 2 at Tier 1; this scenario exercises the transient-stop case where F is retained.)",
            "S-DBN-NXDOMAIN-03 (US-DBN-4): a Tier-3 #[tokio::test] in dns_responder_nxdomain.rs proves that with at least one unrelated server deployed and Running-AND-HEALTHY, a deployed client querying 'nonexistent.svc.overdrive.local' via getent gets NXDOMAIN AND the unrelated server's name still resolves in the same fixture (the miss does not break the hit path on the real socket).",
            "getent-not-dig (K2): every name-path assertion is getent/getaddrinfo from inside the deployed client's netns, never dig @gw alone — the spike litmus that distinguishes a correct source-pin from a broken one.",
            "Merge gate (ADR-0068): the Tier-3 NXDOMAIN ACs assert the pinned-6.18 appliance-kernel Tier-3 matrix; dev-Lima 7.0.0-22-generic is necessary-but-not-sufficient and the surfaces MUST be re-confirmed on 6.18 (DEVOPS/Tier-3 obligation).",
            "Pinned surface ONLY: this step adds NO new production type — it drives the DnsResponder serve loop + answer_for NxDomain arm over the real NameIndex (the WITHHOLD seam) end-to-end through the running-and-healthy gate and the production stop path, AND observes that the FrontendAddrAllocator (01-04) RETAINS the <job> -> F binding across the transient stop (withhold-not-release; release fires only on logical-workload deletion, NOT on the stop). No API beyond the pinned 01-03/01-04/02-00/02-01 surface.",
            "RED convention: Tier-3 scaffolds in dns_responder_nxdomain.rs land via #[should_panic(expected = \"RED scaffold\")] (or #[ignore] only if blocked on an external resource per .claude/rules/testing.md); RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate: the fail-honest NxDomain WITHHOLD logic is already mutation-gated at 01-03 (answer_for NxDomain arm + the NameIndex healthy-gate WITHHOLD seam) and the withhold-not-release retained-F invariant at 01-04 (FrontendAddrAllocator::release only-on-deletion, S-DBN-FRONTEND-03/IDX-02) — both at kill-rate >= 80%; this Tier-3 step adds no new mutable in-process source, so the per-step --file run is a vacuous pass and the per-PR per-package diff run is the backstop. If a small production helper IS added here, it carries its own per-step diff-scoped mutants run at kill-rate >= 80%."
          ],
          "test_file": "crates/overdrive-control-plane/tests/integration/dns_responder_nxdomain.rs",
          "scenario_name": "S-DBN-NXDOMAIN-01/02/03 — NXDOMAIN before-running (withhold then resolve-to-stable-F), after-stop (withhold-not-release, F retained), and for unknown names through getent"
        },
        {
          "id": "03-02",
          "name": "Bidirectional ping-pong demo + EDD expectation (US-DBN-3)",
          "criteria": [
            "S-DBN-PINGPONG (US-DBN-3 · K-DBN-3): a Tier-3 #[tokio::test] in crates/overdrive-control-plane/tests/integration/dns_responder_ping_pong.rs (gated #![cfg(feature=\"integration-tests\")], run via cargo xtask lima run -- cargo nextest run --features integration-tests, NEVER --no-run; is_root() SKIP per K1) proves: GIVEN overdrive serve booted (real dataplane) and the ping-pong bin staged at a real on-disk path (K3) AND examples/dial-by-name-responder/a.toml + b.toml exist with the [service]/[exec]/[resources]/[[listener]] schema (command -> the staged bin, listener ports avoiding 5353 and 53), WHEN Sam runs overdrive deploy a.toml then b.toml, THEN within ~15s of the second deploy A resolves 'b.svc.overdrive.local' (getaddrinfo, NOT dig) and calls B (B's counter increments, date refreshes), within ~15s B resolves 'a.svc.overdrive.local' and calls A (A's counter increments, date refreshes), each hop is intercepted + mTLS'd (TLS 1.3 records on the peer leg, observable via tcpdump / ss -tie), and both counters continue advancing on a ~10s ±5s cadence over a 60s window. Single-example golden bidirectional walkthrough (Mandate 9/11) — no PBT at Tier 3; the two [service].id's (a, b) and the ~10s cadence are the pinned example.",
            "Fixtures: examples/dial-by-name-responder/a.toml and b.toml land (introducing the per-feature examples subdir convention, the coinflip-as-service.toml / dns-resolver.toml precedents) with command pointing at a REAL on-disk staged tiny Rust ping-pong bin (K3 — no phantom paths, the dns-resolver.toml collision class); DELIVER pins the concrete client-program form (staged Rust bin vs python3 -c) to avoid the phantom-path class (reviewer nitpick #7).",
            "EDD graduation (.claude/rules/verification.md): this operator-surface proof graduates to verification/expectations/E05-dial-by-name-ping-pong-mtls (an O/E-surface expectation), anchored to S-DBN-PINGPONG + K-DBN-3, captured black-box against the built overdrive binary under Lima, different-fox-reviewed (never self-stamped). Like sibling E04, the capture is HONEST pending until the full-system EDD harness (#227 / #75) lands — surface that dependency, mirror E04's pending posture, do NOT fabricate a capture.",
            "getent-not-dig (K2): both resolution legs (A->b, B->a) assert via getaddrinfo/getent, never dig @gw alone — the spike litmus.",
            "Merge gate (ADR-0068): the Tier-3 ping-pong AC asserts the pinned-6.18 appliance-kernel Tier-3 matrix; dev-Lima 7.0.0-22-generic is necessary-but-not-sufficient and MUST be re-confirmed on 6.18 (DEVOPS/Tier-3 obligation).",
            "Pinned surface ONLY: this step adds NO new production type — it drives the full responder + intercept loop bidirectionally through two real overdrive deploy invocations against overdrive serve. No API beyond the pinned 01/02 surface.",
            "RED convention: the Tier-3 scaffold in dns_responder_ping_pong.rs lands via #[should_panic(expected = \"RED scaffold\")] (or #[ignore] only if blocked on an external resource per .claude/rules/testing.md); RED->GREEN->COMMIT per ADR-025, gated by distill/red-classification.md.",
            "Mutation gate: EXEMPT — this step lands docs/example .toml fixtures + a staged demo bin + a Tier-3 acceptance test over already-gated production logic, with no new mutable in-process production source; per CLAUDE.md/testing.md a docs/example-only step has nothing for the per-step mutants run to mutate (vacuous pass). The per-PR per-package diff run is the backstop for any incidental logic change."
          ],
          "test_file": "crates/overdrive-control-plane/tests/integration/dns_responder_ping_pong.rs",
          "scenario_name": "S-DBN-PINGPONG — two services dial each other by name, counters advance, each hop mTLS'd (graduates to E05 EDD, honest pending)"
        }
      ]
    }
  ],
  "implementation_scope": {
    "source_directories": [
      "crates/overdrive-core/src/",
      "crates/overdrive-control-plane/src/dns_responder/",
      "crates/overdrive-control-plane/src/mtls_resolve_adapter.rs",
      "crates/overdrive-control-plane/src/lib.rs",
      "examples/dial-by-name-responder/"
    ],
    "test_directories": [
      "crates/overdrive-core/tests/acceptance/",
      "crates/overdrive-control-plane/tests/acceptance/",
      "crates/overdrive-control-plane/tests/integration/"
    ],
    "excluded_patterns": [
      "target/**",
      "**/*.proptest-regressions"
    ]
  },
  "validation": {
    "status": "pending_re_review",
    "reviewer": "nw-solution-architect-reviewer",
    "approved_at": null,
    "superseded_review": {
      "note": "The prior nw-solution-architect-reviewer APPROVED verdict (2026-06-24T20:10:04Z) applied to the SUPERSEDED REV-1 26-scenario / 7-step spec (headless backend-addr answer contract). Per feature-delta REV-2 wave-decision #7, that verdict does NOT carry forward — the Final Wave Review Gate MUST re-run against this REV-2 39-scenario / 9-step stable-frontend spec. Recorded for the audit trail only.",
      "rev1_verdict": "APPROVED",
      "rev1_approved_at": "2026-06-24T20:10:04Z",
      "rev1_scope": "REV-1 (26 scenarios, 7 steps) — superseded by ADR-0072 REV-2 stable-frontend / ClusterIP split"
    },
    "review": {
      "type": "roadmap-vs-feature-artifacts fidelity review (REV-2 — re-review PENDING)",
      "verdict": "PENDING_RE_REVIEW",
      "summary": "REV-2 rewrite operationalizes ADR-0072 REV-2 (stable-frontend / ClusterIP split), the re-DISTILLed 39-scenario executable spec (distill/test-scenarios.md), and the feature-delta REV-2 per-step re-scope table. The roadmap now carries 9 steps: 01-01/01-02 COMMITTED unchanged (addr-agnostic), 01-04 NEW (FrontendAddrAllocator, 1a-A), 01-03 RE-SCOPED (<job>->stable F, withhold-not-release), 02-00 NEW (MtlsResolve re-key, 1b-A), 02-01 EXTENDED (+FrontendAddrAllocator + re-keyed resolve wiring), 02-02 RE-SCOPED (stable-F + churn ACs), 03-01 RE-DISTILLed (withhold-not-release), 03-02 unchanged. All 39 S-DBN-* scenarios trace bidirectionally to their owning step(s). OQ-1 remains a crafter DECISION/BLOCKER. Pending the Final Wave Review Gate re-run against the REV-2 spec.",
      "blocking_issues_in_roadmap": [],
      "carried_forward_to_deliver": [
        {
          "id": "OQ-1",
          "title": "SpiffeId -> <job> accessor signature (CONFIRMED-ABSENT today, STILL needed under REV-2)",
          "location": "step 01-03 (name_index.rs) — now also keys the FrontendAddrAllocator by <job> (01-04) + groups rows",
          "status": "Correctly encoded by the roadmap as a crafter DECISION/BLOCKER, NOT a roadmap defect. REV-2 STILL needs it (to key the per-<job> frontend allocator and group running-and-healthy rows). The crafter MUST pin the exact accessor shape (a new SpiffeId::job_segment() -> Option<&str> on the newtype, OR a parse helper local to name_index.rs) as a DECISION before writing production code; return a BLOCKER if ambiguous (CLAUDE.md 'Implement to the design — never invent API surface'). id.rs:269-304 exposes only as_str/trust_domain/path + for_allocation."
        },
        {
          "id": "OQ-REV2-A",
          "title": "FrontendKey newtype-vs-tuple shape",
          "location": "step 02-00 (mtls_resolve_adapter.rs)",
          "status": "DELIVER-open detail (NAMED, not picked by DISTILL/DESIGN). FrontendKey = (SocketAddrV4, Proto) MAY be a named newtype for call-site clarity; the CONTRACT is that the key discriminates proto (Finding-1). The crafter pins the shape as a DECISION; no scenario depends on a specific shape (they assert behaviour)."
        },
        {
          "id": "OQ-REV2-B",
          "title": "CIDR-membership accessor for orig_dst.ip() in 10.98.0.0/16",
          "location": "step 02-00 (the fail-closed-on-frontend-subnet-miss arm)",
          "status": "DELIVER-open detail (NAMED, not picked). An Ipv4Net::contains on WORKLOAD_FRONTEND_BASE or an equivalent mask compare; the crafter MUST NOT invent a broader 'is this any reserved subnet' helper — the test is specifically membership in the dial-by-name frontend block (REV-2 § Frontend-subnet coherence contract)."
        }
      ],
      "coverage_matrix": {
        "_note": "REV-2 — 39 S-DBN-* scenarios mapped to 9 steps. 13 PRESERVED, 13 RE-DISTILLed, 13 NEW (FRONTEND-01..04, REKEY-01..04, FAILCLOSED-01, COHERENCE-01, EQUIV-01, WS-STABLE, CHURN).",
        "DDN-1_sibling_reader_SUPERSEDED_by_additive_extend": "01-03 (sibling name_index) + 02-00 (REV-2: BackendIndex EXTENDED additively with by_frontend/classify; REV-1 'untouched' superseded, user-ratified)",
        "DDN-2_healthy_gate_RE-SCOPED_to_WITHHOLD_seam": "01-03 (ANSWER-04, IDX-02 — governs resolvability, not which addr); selection-time healthy in 02-00 (REKEY-01/02)",
        "DDN-3_hickory_proto_codec": "01-02 (wire.rs, COMMITTED)",
        "DDN-4_pure_answer_for_seam": "01-03 (primary mutation target)",
        "DDN-5_wildcard_first_per_addr_fallback": "02-01 (BIND-01/02)",
        "DDN-6_run_server_wiring_earned_trust": "02-01 (BIND-03; EXTENDED with FrontendAddrAllocator + re-keyed resolve construct)",
        "DDN-7_MeshServiceName_newtype": "01-01 (NAME-*, COMMITTED)",
        "DDN-8_NXDOMAIN_NODATA_1s_SOA": "01-02 (wire), 03-01 (e2e)",
        "REV-2_1a-A_FrontendAddrAllocator": "01-04 (FRONTEND-01..04)",
        "REV-2_1b-A_by_frontend_translation": "02-00 (REKEY-01..04)",
        "REV-2_Finding-1_FrontendKey_proto_axis": "02-00 (REKEY-03)",
        "REV-2_Finding-2_withhold-not-release": "01-04 (FRONTEND-03), 01-03 (IDX-02), 02-02 (WS-STABLE), 03-01 (NXDOMAIN-02)",
        "REV-2_Finding-3_fail-closed-on-subnet-miss": "02-00 (FAILCLOSED-01)",
        "REV-2_Finding-3i_ordered-drain-coherence": "02-00 (COHERENCE-01)",
        "REV-2_equivalence_trait_docstring_contract": "02-00 (EQUIV-01)",
        "REV-2_active-termination_no_sock_destroy": "02-02 (CHURN; sock_destroy is #61 scope, NOT here)",
        "REV-2_SQ1-elimination_stable-F": "01-04 (FRONTEND-02 Tier-1), 02-02 (WS-STABLE Tier-3)",
        "REV-2_D-DBN-6_frontend_backend_translation_invariant": "02-00 (REKEY-01), 02-02 (SINGLE-SRC)",
        "REV-2_BLOCKER-1_dataplane_capture": "RESOLVED -> WORKS (spike); no nft extension; exercised by 02-02 (WS) capture of F",
        "REV-2_BLOCKER-2_multi-replica_selection": "RESOLVED -> deterministic first-by-Ord, pinned in 02-00 (REKEY-01)",
        "REV-2_subnet_collision_check": "RESOLVED -> WORKLOAD_FRONTEND_BASE = 10.98.0.0/16 (disjoint from VIP 10.96.0.0/16 + workload 10.99.0.0/16), asserted in 01-04 (FRONTEND-01)",
        "OQ-1_spiffeid_job_accessor": "01-03 + 01-04 (pinned as crafter BLOCKER, STILL open under REV-2)",
        "OQ-3_gateway_source": "RESOLVED in DESIGN -> NetSlotAllocator in 02-01 surface",
        "US-DBN-1_spike": "pre-DELIVER (PROMOTED)",
        "US-DBN-2_walking_skeleton": "01-04, 01-03, 02-00, 02-01, 02-02 (WS, WS-STABLE, SINGLE-SRC)",
        "US-DBN-3_ping_pong": "03-02 (PINGPONG + E05)",
        "US-DBN-4_empty_candidate_honesty": "01-04 (FRONTEND-03), 02-00 (REKEY-02), 02-02 (CHURN), 03-01 (NXDOMAIN-*)",
        "K-DBN-1_resolves_to_live_backend": "02-02 (WS, WS-STABLE)",
        "K-DBN-2_empty_candidate_honesty": "02-02 (CHURN), 03-01 (NXDOMAIN-*)",
        "K-DBN-3_operator_runnable_ping_pong": "03-02 (PINGPONG + E05)",
        "K-DBN-4_single_source_consistency": "01-03 (IDX-04 in-memory half), 02-00 (REKEY-01, FAILCLOSED-01), 02-02 (SINGLE-SRC Tier-3 oracle)",
        "S-DBN-NAME-01": "01-01",
        "S-DBN-NAME-02": "01-01",
        "S-DBN-NAME-03": "01-01",
        "S-DBN-NAME-04": "01-01",
        "S-DBN-WIRE-01": "01-02",
        "S-DBN-WIRE-02": "01-02",
        "S-DBN-WIRE-03": "01-02",
        "S-DBN-WIRE-04": "01-02",
        "S-DBN-FRONTEND-01": "01-04",
        "S-DBN-FRONTEND-02": "01-04",
        "S-DBN-FRONTEND-03": "01-04",
        "S-DBN-FRONTEND-04": "01-04",
        "S-DBN-ANSWER-01": "01-03",
        "S-DBN-ANSWER-02": "01-03",
        "S-DBN-ANSWER-03": "01-03",
        "S-DBN-ANSWER-04": "01-03",
        "S-DBN-ANSWER-05": "01-03",
        "S-DBN-IDX-01": "01-03",
        "S-DBN-IDX-02": "01-03",
        "S-DBN-IDX-03": "01-03",
        "S-DBN-IDX-04": "01-03",
        "S-DBN-REKEY-01": "02-00",
        "S-DBN-REKEY-02": "02-00",
        "S-DBN-REKEY-03": "02-00",
        "S-DBN-REKEY-04": "02-00",
        "S-DBN-FAILCLOSED-01": "02-00",
        "S-DBN-COHERENCE-01": "02-00 (the ordered drain feeds both projections; test home dns_name_index.rs)",
        "S-DBN-EQUIV-01": "02-00",
        "S-DBN-BIND-01": "02-01",
        "S-DBN-BIND-02": "02-01",
        "S-DBN-BIND-03": "02-01",
        "S-DBN-WS": "02-02",
        "S-DBN-WS-STABLE": "02-02",
        "S-DBN-SINGLE-SRC": "02-02",
        "S-DBN-CHURN": "02-02",
        "S-DBN-NXDOMAIN-01": "03-01",
        "S-DBN-NXDOMAIN-02": "03-01",
        "S-DBN-NXDOMAIN-03": "03-01",
        "S-DBN-PINGPONG": "03-02",
        "coverage_summary": "39/39 S-DBN-* scenarios mapped (13 PRESERVED + 13 RE-DISTILLed + 13 NEW); 0 dropped. NOTE: a dispatch-vs-SSOT contradiction was surfaced (the dispatch enumerated PINGPONG + NXDOMAIN-01/02 under 02-02 and BIND-01/02/03 under 03-01); resolved AGAINST the dispatch toward the binding DISTILL test-scenarios.md test-file mapping (PINGPONG->03-02 dns_responder_ping_pong.rs; NXDOMAIN-01/02/03->03-01 dns_responder_nxdomain.rs; BIND-01/02/03->02-01 dns_responder_bind.rs). See the orchestrator BLOCKER note in the revision return."
      }
    }
  }
}
