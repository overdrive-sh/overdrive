# ADR-0086 S2-gate characterization golden — reconciler `workflow_lifecycle`
# target: workflow/wf-app
# captured from the PRE-MOVE central hydrate_desired/hydrate_actual free
# fns (reconciler_runtime.rs), step 02-03, before the S3 (02-04) cut.
# expected AnyState variant: WorkflowLifecycle

== hydrate_desired -> AnyState::WorkflowLifecycle ==
WorkflowLifecycle(
    WorkflowLifecycleState {
        instances: {},
    },
)

== hydrate_actual -> AnyState::WorkflowLifecycle ==
WorkflowLifecycle(
    WorkflowLifecycleState {
        instances: {},
    },
)
