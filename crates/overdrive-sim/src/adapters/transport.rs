//! `SimTransport` — turmoil-wrapped network with injectable partition /
//! loss / delay.
//!
//! SCAFFOLD: true — DISTILL placeholder per DWD-06. Step 05-01 fills
//! this in against `overdrive_core::traits::Transport`.

pub struct SimTransport;
