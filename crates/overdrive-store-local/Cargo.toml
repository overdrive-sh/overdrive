# SCAFFOLD: true — DISTILL placeholder per DWD-06. Crafter completes in place.
#
# `overdrive-store-local` hosts the real redb-backed `LocalStore` adapter
# (per the architecture brief §3 crate topology and the US-03 reuse
# table row 20). Real redb is added by the crafter during DELIVER.

[package]
name        = "overdrive-store-local"
version     = "0.0.0"
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true
authors.workspace     = true
repository.workspace  = true
publish               = false

[package.metadata.overdrive]
crate_class = "adapter-real"

[dependencies]
overdrive-core.workspace = true

[dev-dependencies]
# Crafter adds tempfile, proptest during DELIVER.

[lints]
workspace = true
