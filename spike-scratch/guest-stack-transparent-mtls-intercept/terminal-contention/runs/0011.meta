step=inspect-prior-marker-owned-partial
started_utc=2026-08-31T21:39:56.499203000Z
ended_utc=2026-08-31T21:40:19.426202000Z
exit_code=1
command=OVERDRIVE_METAL_KERNEL=/srv/vm/overdrive-testing/kernel OVERDRIVE_METAL_ROOTFS=/srv/vm/overdrive-testing/rootfs.ext4 OVERDRIVE_METAL_SCENARIO=terminal-contention-recover-inspect cargo xtask metal run -- bash -lc 'set -euo pipefail; root=/srv/vm/overdrive-testing/gti-e07; printf "root=%s\\n" "$root"; if [[ -f "$root/.gti-e07-owned" ]]; then sed "s/^/marker=/" "$root/.gti-e07-owned"; fi; if [[ -d "$root/probe-run-1" ]]; then find "$root/probe-run-1" -maxdepth 2 -printf "%y %p\\n" | sort; fi; for f in "$root/probe-run-1/session-wrapper.ready" "$root/probe-run-1/session-wrapper.ack" "$root/probe-run-1/serve.pid" "$root/probe-run-1/serve.log"; do if [[ -e "$f" ]]; then printf "--- %s ---\\n" "$f"; sed -n "1,80p" "$f"; fi; done'
