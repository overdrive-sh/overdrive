# E06 execution substrate
#
# transport:        cargo xtask metal run --   (NOT cargo xtask lima run --)
# why:              S-VM-39 boots a real Cloud Hypervisor guest; that needs
#                   x86_64 + nested KVM, which Lima on Apple Silicon cannot
#                   provide (.claude/rules/testing.md § bare-metal KVM box).
# target:           $OVERDRIVE_METAL_TARGET (host redacted in evidence)
# declared:         execution-substrate = native-metal
# executed_in_lima: false in fresh verification.yaml captures
# build:            cargo build -p overdrive-cli --bin overdrive
#                   (DEFAULT features — no integration-tests, no kvm-tests)
