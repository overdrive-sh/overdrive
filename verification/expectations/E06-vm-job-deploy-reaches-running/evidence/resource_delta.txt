# Allocation-scoped VM resources that appeared during this run.
# Non-zero on all three is the black-box form of the in-tree
# assertion that the production VmDriver path RAN (rather than the
# parser merely accepting the spec).
new_hypervisors=1
new_run_dirs=1
new_scopes=1
