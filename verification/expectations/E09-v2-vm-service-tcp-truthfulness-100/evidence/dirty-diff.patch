diff --git a/AGENTS.md b/AGENTS.md
index 538475a8..da693543 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -286,7 +286,7 @@ directory listed above.
 <claude-mem-context>
 # Memory Context
 
-# [helios/accra] recent context, 2026-09-08 12:33pm GMT+2
+# [helios/accra] recent context, 2026-09-08 5:14pm GMT+2
 
 Legend: 🎯session 🔴bugfix 🟣feature 🔄refactor ✅change 🔵discovery ⚖️decision 🚨security_alert 🔐security_note
 Format: ID TIME TYPE TITLE
diff --git a/docs/feature/service-kind-vm-workloads/deliver/execution-log.json b/docs/feature/service-kind-vm-workloads/deliver/execution-log.json
index a67034a1..fe13c569 100644
--- a/docs/feature/service-kind-vm-workloads/deliver/execution-log.json
+++ b/docs/feature/service-kind-vm-workloads/deliver/execution-log.json
@@ -218,6 +218,20 @@
       "s": "EXECUTED",
       "d": "PASS",
       "t": "2026-09-08T12:51:11Z"
+    },
+    {
+      "sid": "02-04",
+      "p": "RED",
+      "s": "EXECUTED",
+      "d": "FAIL",
+      "t": "2026-09-08T14:04:43Z"
+    },
+    {
+      "sid": "02-04",
+      "p": "GREEN",
+      "s": "EXECUTED",
+      "d": "FAIL",
+      "t": "2026-09-08T14:40:14Z"
     }
   ]
 }
\ No newline at end of file
diff --git a/examples/service-kind-vm-workloads-v2/run-example.sh b/examples/service-kind-vm-workloads-v2/run-example.sh
index b90b7c42..68bf77b4 100755
--- a/examples/service-kind-vm-workloads-v2/run-example.sh
+++ b/examples/service-kind-vm-workloads-v2/run-example.sh
@@ -324,6 +324,34 @@ assert_case_resources_released() {
   }
 }
 
+workload_runtime_resources_remain() {
+  local id="$1"
+  local allocation_prefix="alloc-$id-"
+  if probe_scopes | grep -F "$allocation_prefix" >/dev/null; then
+    return 0
+  fi
+  if probe_run_dirs | grep -F "$allocation_prefix" >/dev/null; then
+    return 0
+  fi
+  if probe_clone_state | grep -F "$allocation_prefix" >/dev/null; then
+    return 0
+  fi
+  return 1
+}
+
+wait_for_workload_runtime_cleanup() {
+  local id="$1"
+  local deadline=$((SECONDS + 60))
+  while [[ "$SECONDS" -lt "$deadline" ]]; do
+    assert_serve_identity
+    if ! workload_runtime_resources_remain "$id"; then
+      return 0
+    fi
+    sleep 1
+  done
+  return 1
+}
+
 first_service_alloc_state() {
   awk '
     /^Alloc[[:space:]]+State[[:space:]]+/ { in_table = 1; next }
@@ -642,10 +670,13 @@ wait_for_job_success() {
   while [[ "$SECONDS" -lt "$deadline" ]]; do
     assert_serve_identity
     query_describe "$id" "$output" || true
-    if [[ "$(first_job_attempt_state <"$output")" == "Terminated" ]] \
+    local state
+    state="$(first_job_attempt_state <"$output")"
+    if [[ "$state" == "Terminated" ]] \
       && grep -Fq 'Verdict: Succeeded' "$output"; then
       return 0
     fi
+    [[ "$state" == "Failed" || "$state" == "Stopped" ]] && return 1
     sleep 1
   done
   return 1
@@ -667,10 +698,16 @@ stop_workload() {
   while [[ "$SECONDS" -lt "$deadline" ]]; do
     assert_serve_identity
     query_describe "$id" "$describe" || true
-    [[ "$(first_service_alloc_state <"$describe")" == "Terminated" ]] \
-      && return 0
-    [[ "$(first_job_attempt_state <"$describe")" == "Terminated" ]] \
-      && return 0
+    local service_state job_state
+    service_state="$(first_service_alloc_state <"$describe")"
+    job_state="$(first_job_attempt_state <"$describe")"
+    if [[ "$service_state" == "Terminated" || "$service_state" == "Failed" \
+      || "$service_state" == "Stopped" ]] \
+      || [[ "$job_state" == "Terminated" || "$job_state" == "Failed" \
+      || "$job_state" == "Stopped" ]]; then
+      wait_for_workload_runtime_cleanup "$id" || return 1
+      return 0
+    fi
     sleep 1
   done
   return 1
@@ -1097,9 +1134,11 @@ print_case_transcript() {
   fi
   local file
   for file in healthy-deploy.out healthy-describe.out healthy-client-deploy.out \
-    healthy-client-describe.out healthy-client-stop.out healthy-stop.out \
+    healthy-client-describe.out healthy-client-stop.out healthy-client-stop-describe.out \
+    healthy-stop.out \
     healthy-stop-describe.out failure-deploy.out failure-describe.out \
     failure-client-deploy.out failure-client-describe.out failure-client-stop.out \
+    failure-client-stop-describe.out \
     failure-stop.out failure-final-describe.out healthy-query-errors \
     failure-query-errors healthy-resources-active healthy-resources-after \
     failure-resources-active failure-resources-after; do
diff --git a/verification/expectations/E09-v2-vm-service-tcp-truthfulness-100/runner.sh b/verification/expectations/E09-v2-vm-service-tcp-truthfulness-100/runner.sh
index 190dd11b..1d86a468 100755
--- a/verification/expectations/E09-v2-vm-service-tcp-truthfulness-100/runner.sh
+++ b/verification/expectations/E09-v2-vm-service-tcp-truthfulness-100/runner.sh
@@ -22,7 +22,7 @@ trap cleanup EXIT HUP INT TERM
 } >"$EVIDENCE_DIR/product-run.meta"
 
 set +e
-timeout 14400s cargo xtask metal run -- bash -lc \
+timeout 360s cargo xtask metal run -- bash -lc \
   "SVM_E09_V2_OUTPUT_ROOT=$V2_OUTPUT_ROOT $EXAMPLE run tcp-truthfulness-100" \
   >"$RAW_OUTPUT" 2>&1
 run_rc=$?
