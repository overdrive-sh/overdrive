#!/usr/bin/env bash
# E08 pending EDD stub. Roadmap/DEVOPS must supply the built-binary native-metal
# runner, universal Run/Sync/direct-bootstrap lease, deterministic INPUT-hook
# base-chain production `append-egress`/`append-rule -EOPNOTSUPP` failure,
# assertion-safe exact fixture restoration, resolver failure, exit-78
# complement, deadlines, cleanup, and sibling-preservation probes. A transient
# durable Running row is permitted; final Failed and no EXEC are mandatory.
set -uo pipefail
echo "  [pending] E08 needs the roadmap-owned native x86_64 metal lifecycle runner."
echo "            No command was executed and no evidence was fabricated."
exit 0
